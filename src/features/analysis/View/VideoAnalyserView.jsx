// src/features/analysis/View/VideoAnalyzerView.jsx
const VideoAnalyzerView = ({ vm }) => {
  const {
    videoFile, frames, selectedFrames,
    loadVideo, toggleFrameSelection,
  } = vm;

  // UI för att välja video, visa frames, klicka för att välja
};
